# 🚀 Руководство по запуску

## ✅ Исправленные проблемы

### 1. Electron версия (белый экран)
- **Исправлено**: Добавлена задержка перед загрузкой страницы
- **Исправлено**: Автоматический fallback на демо-данные
- **Исправлено**: Улучшена обработка ошибок

### 2. Полная веб-версия
- **Исправлено**: Упрощен server.js с fallback на демо-данные
- **Исправлено**: Убраны проблемные импорты API модулей
- **Исправлено**: Добавлена обработка ошибок базы данных

## 🎯 Рекомендуемый способ запуска

### Для демонстрации (рекомендуется)
```bash
npm run simple
```
Откройте http://localhost:3000

### Для разработки
```bash
npm run web
```
Откройте http://localhost:3000

### Для macOS приложения
```bash
npm start
```

## 🔧 Что исправлено

### Electron версия
- Окно ждет 2 секунды перед загрузкой страницы
- Автоматический fallback на демо-данные при проблемах с БД
- Улучшенная обработка ошибок

### Веб-версия
- Упрощенный server.js без проблемных импортов
- Встроенные демо-данные
- Автоматический fallback при ошибках

### Общие улучшения
- Лучшая обработка ошибок
- Информативные сообщения в консоли
- Автоматическое переключение на демо-режим

## 🎭 Функциональность

### Работает в демо-режиме:
- ✅ Просмотр оборудования
- ✅ Просмотр категорий
- ✅ Поиск и фильтрация
- ✅ Статистика
- ✅ История действий
- ✅ Аутентификация (автоматический вход как admin)

### Ограничения демо-режима:
- ❌ Добавление оборудования (показывает сообщение об ошибке)
- ❌ Редактирование данных
- ❌ Удаление данных
- ❌ Сохранение изменений

## 🚀 Быстрый старт

1. **Установите зависимости**:
   ```bash
   npm install
   ```

2. **Запустите приложение**:
   ```bash
   npm run simple
   ```

3. **Откройте браузер**:
   http://localhost:3000

4. **Войдите в систему**:
   - Логин: admin
   - Пароль: admin123
   - (или автоматический вход в демо-режиме)

## 🔍 Проверка работоспособности

### В браузере должно отображаться:
- ✅ Заголовок "Склад СПО"
- ✅ Навигационное меню слева
- ✅ Статистика на главной странице
- ✅ Список оборудования
- ✅ Категории с подкатегориями
- ✅ История действий

### В консоли терминала должно быть:
```
🎭 Theater Lighting Warehouse запущен на http://localhost:3000
📱 Откройте браузер и перейдите по адресу выше
🔧 Для остановки нажмите Ctrl+C
⚠️ Работает в демо-режиме (база данных недоступна)
```

## 🆘 Если что-то не работает

1. **Проверьте порт 3000**:
   ```bash
   lsof -i :3000
   ```

2. **Остановите все процессы**:
   ```bash
   pkill -f "node.*server"
   ```

3. **Перезапустите**:
   ```bash
   npm run simple
   ```

4. **Проверьте консоль браузера** (F12 → Console)

## 📱 Тестирование функций

### Главная страница
- Статистика по оборудованию
- Последние действия
- Навигация по разделам

### Оборудование
- Просмотр списка оборудования
- Поиск по названию
- Фильтрация по категориям и статусу
- Сортировка

### Категории
- Иерархическое отображение
- Подсчет оборудования
- Навигация по подкатегориям

### История
- Лог всех действий
- Временные метки
- Информация о пользователях

## 🎉 Готово!

Приложение должно работать стабильно в демо-режиме. Все основные функции интерфейса доступны для тестирования и демонстрации.

