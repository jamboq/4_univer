# 🔧 Устранение неполадок

## Проблемы и решения

### 1. Electron версия показывает белый экран

**Проблема**: При запуске `npm start` открывается окно Electron, но показывается только белый экран.

**Причины и решения**:

#### A. Сервер не успевает запуститься
**Решение**: Окно теперь ждет 2 секунды перед загрузкой страницы. Если проблема остается:
```bash
# Увеличьте время ожидания в src/main.js (строка 42)
setTimeout(() => {
    mainWindow.loadURL('http://localhost:3000');
    mainWindow.show();
}, 3000); // Увеличьте с 2000 до 3000
```

#### B. Порт 3000 занят
**Решение**: 
```bash
# Проверьте, что порт свободен
lsof -i :3000

# Если занят, убейте процесс
kill -9 <PID>

# Или измените порт в src/main.js
server = app.listen(3001, () => {
```

#### C. Проблемы с базой данных
**Решение**: Приложение автоматически переключается на демо-режим, если база данных недоступна.

### 2. Полная веб-версия не запускается

**Проблема**: `npm run web` не работает или показывает ошибки.

**Причины и решения**:

#### A. Ошибки импорта модулей
**Решение**: Теперь server.js использует fallback на демо-данные:
```bash
# Запустите полную версию
npm run web

# Если не работает, используйте простую версию
npm run simple
```

#### B. Проблемы с SQLite
**Решение**: 
```bash
# Установите sqlite3
npm install sqlite3

# Или используйте демо-режим
npm run simple
```

#### C. Порт занят
**Решение**:
```bash
# Используйте другой порт
PORT=3001 npm run web
```

### 3. Ошибки в консоли браузера

**Проблема**: В консоли браузера появляются ошибки JavaScript.

**Решения**:

#### A. Ошибки CORS
**Решение**: CORS настроен в server.js. Если проблемы остаются:
```javascript
// В server.js добавьте:
app.use(cors({
    origin: '*',
    credentials: true
}));
```

#### B. Ошибки загрузки ресурсов
**Решение**: Убедитесь, что все файлы находятся в правильных местах:
```
public/
├── index.html
├── css/styles.css
└── js/
    ├── app.js
    ├── equipment.js
    ├── categories.js
    └── auth.js
```

### 4. Проблемы с аутентификацией

**Проблема**: Не удается войти в систему или появляются ошибки авторизации.

**Решения**:

#### A. Демо-режим
**Решение**: В демо-режиме автоматически авторизуется пользователь admin:
- Логин: admin
- Пароль: admin123

#### B. Проблемы с токенами
**Решение**: Очистите localStorage:
```javascript
// В консоли браузера:
localStorage.clear();
```

### 5. Проблемы с отображением

**Проблема**: Интерфейс отображается неправильно или стили не загружаются.

**Решения**:

#### A. Стили не загружаются
**Решение**: Проверьте путь к CSS файлу в index.html:
```html
<link rel="stylesheet" href="css/styles.css">
```

#### B. Шрифты не загружаются
**Решение**: Приложение использует системные шрифты, но можно добавить Google Fonts:
```html
<link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
```

### 6. Проблемы с производительностью

**Проблема**: Приложение работает медленно или зависает.

**Решения**:

#### A. Большое количество данных
**Решение**: Ограничьте количество записей:
```javascript
// В API добавьте пагинацию
const limit = req.query.limit || 50;
const offset = req.query.offset || 0;
```

#### B. Медленные запросы к базе данных
**Решение**: Добавьте индексы в базу данных или используйте демо-режим.

## 🚀 Быстрые решения

### Полный сброс
```bash
# Остановите все процессы
pkill -f "node.*server"
pkill -f "electron"

# Очистите кэш
rm -rf node_modules
npm install

# Запустите заново
npm run simple
```

### Проверка работоспособности
```bash
# 1. Проверьте Node.js
node --version

# 2. Проверьте зависимости
npm list

# 3. Запустите простую версию
npm run simple

# 4. Откройте http://localhost:3000
```

### Логи и отладка
```bash
# Запуск с подробными логами
DEBUG=* npm run web

# Или для Electron
NODE_ENV=development npm start
```

## 📞 Получение помощи

Если проблемы остаются:

1. **Проверьте консоль браузера** (F12 → Console)
2. **Проверьте терминал** на ошибки сервера
3. **Убедитесь в правильности путей** к файлам
4. **Попробуйте демо-режим** (`npm run simple`)

### Полезные команды для диагностики

```bash
# Проверка портов
netstat -an | grep 3000

# Проверка процессов Node.js
ps aux | grep node

# Проверка файлов
ls -la public/
ls -la src/

# Проверка зависимостей
npm audit
npm outdated
```

## ✅ Чек-лист работоспособности

- [ ] Node.js установлен (версия 16+)
- [ ] Зависимости установлены (`npm install`)
- [ ] Порт 3000 свободен
- [ ] Файлы на месте (public/, src/)
- [ ] Нет ошибок в консоли браузера
- [ ] Нет ошибок в терминале
- [ ] Приложение открывается в браузере

